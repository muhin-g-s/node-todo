export type Auth = {
	userId: string;
	token: string;
	username: string;
}

export type Login = {
	username: string;
	password: string;
}

export type Register = {
	username: string;
	password: string;
}